# C2.3 engine test inventory

Captured from AI Shipping Labs at the C2.3 implementation baseline on 2026-09-05.

The donor engine baseline is 112 tests. The following files exercise reusable engine behavior and
exclude site content parsers and domain model behavior:

| Donor file | Tests |
|---|---:|
| `test_content_checkout_boundary.py` | 30 |
| `test_content_sync_queue.py` | 11 |
| `test_github_sync_lifecycle.py` | 4 |
| `test_github_sync_pipeline_refactor.py` | 3 |
| `test_github_webhook_deduplication.py` | 20 |
| `test_sync_content_command.py` | 10 |
| `test_sync_head_sha_skip.py` | 24 |
| `test_sync_observability.py` | 10 |

`test_github_sync.py`, dispatcher tests and content-model tests are excluded because they combine
AISL-owned parser and domain behavior. Equivalent package behavior is adapted under
`tests/content_sync/`; it is not copied with AISL imports.

Verify the package count and behavior with:

```text
uv run pytest tests/content_sync --collect-only -q
make test tests/content_sync
```
