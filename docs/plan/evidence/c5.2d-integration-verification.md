# C5.2d integration verification evidence

Captured: 2026-09-08. Independent gate runs over the merged C5.2d split, recorded by a
coordinator session that implemented none of the verified code. All runs used throwaway
detached worktrees under /tmp with unique SQLite database paths; the shared repository
working tree was never modified.

## Scope

| Pull request | Issue | Merge commit |
|---|---|---|
| #138 C5.2db Registration campaigns and course registrations | C5.2db | `89c7e6c` |
| #141 C5.2da Leaderboard rollup, preferences and complaints | C5.2da | `5470d33` |
| #146 C5.2dc Learner views, member APIs and certificates | C5.2dc | `121e7b0` |

The status rows damaged by the split were repaired by #150 and C5.2dc was marked done by
#151; the main tip at capture time is `6433f12`.

## Gates on main at `36f461f` (after C5.2da)

| Gate | Result |
|---|---|
| `uv run --frozen pytest tests -q` (full package suite) | 1042 passed, 3 skipped, 0 failed |
| `uv run --frozen ruff check .` | All checks passed |
| `uv run --frozen ruff format --check .` | 568 files already formatted |
| `uv run --frozen python testproject/manage.py check` | No issues |
| `makemigrations --check --dry-run` | No changes detected |
| `uv run --frozen python scripts/plan.py check` | OK: 93 issues, STATUS.md consistent |

## Gates on main at `6433f12` (after C5.2db and C5.2dc)

| Gate | Result |
|---|---|
| `uv run --frozen pytest tests -q` (full package suite) | 1061 passed, 3 skipped, 0 failed |
| `uv run --frozen ruff check .` | All checks passed |
| `uv run --frozen ruff format --check .` | 575 files already formatted |
| `uv run --frozen python testproject/manage.py check` | No issues |
| `makemigrations --check --dry-run` | No changes detected |
| `uv run --frozen python scripts/plan.py check` | OK: 93 issues, STATUS.md consistent |

## Gates on the C5.2dc branch at `71d6353` (pre-merge)

| Gate | Result |
|---|---|
| `uv run --frozen pytest tests/coursework -q` | 91 passed |
| `uv run --frozen ruff check .` | All checks passed |
| `uv run --frozen ruff format --check .` | 569 files already formatted |
| `uv run --frozen python testproject/manage.py check` | No issues |
| `makemigrations --check --dry-run` | No changes detected |
| `uv run --frozen pytest tests/test_boundaries.py -q` | 1 passed |
| `uv run --frozen python scripts/plan.py check` | OK: 93 issues, STATUS.md consistent |

Before the merge, the branch conflicted with then-main on
`community_base/coursework/leaderboard.py` (both sides had moved it); the merge that
landed as `121e7b0` carried the resolution.

## Deferred compatibility gates

Not run here, needs: donor equivalence, development-copy rehearsals and site deploys stay
with the adoption issues (D5.1, D5.2 and A5.1, A5.2), per the plan's separation of package
capability from adoption compatibility.
