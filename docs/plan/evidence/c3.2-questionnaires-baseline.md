# C3.2 questionnaires donor baseline

Package-local adaptation starts from AI Shipping Labs commit
`81c6c1e4d95ed51d09368a41b99e64841ddfb43a`.

The donor `questionnaires` app contains:

- 8,978 Python lines outside bytecode caches;
- 198 `test_` methods across 15 test files;
- eight numbered migrations from `0001_initial` through `0008_response_review_queue`.

C3.2 will classify every donor test file as adapted, mixed or site-owned/deferred before claiming
package-local completion. The migration generated in this issue is provisional. Exact schema and
migration-history equivalence, a development-copy rehearsal and the permanent `replaces` list remain
C3.7 compatibility gates.
