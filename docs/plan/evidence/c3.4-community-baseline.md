# C3.4 AISL community baseline

Captured from AISL commit `81c6c1e4d95ed51d09368a41b99e64841ddfb43a` before package edits.

- Python inventory: 11,850 lines under `community/`, including tests and migrations.
- Migration inventory: 19 numbered migrations, `0001_initial` through
  `0019_alter_callhost_options_alter_bookedcall_host_and_more`.
- Automated inventory: 309 test methods across the 20 top-level `test_*.py` files, plus one
  credential-gated live Slack announcement test.
- Model modules: audit log, call host, booked call and unmatched booked call.
- Slack modules: API service, configuration, profile links, user import, membership refresh,
  invite/reactivate/removal tasks and staff notifications.
- Calendly modules: configuration, signed webhook processing and booked-call projection.

The package-first stage records behavior coverage but doesn't claim migration equivalence. C3.7
will compare the provisional kept-label migration with the prepared AISL migration state before any
tag contains the community schema.
