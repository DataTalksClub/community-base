# Decisions

Taken by the owner on 2026-09-05. An executor does not re-open these. If a step in the plan
appears to contradict one of them, the plan is wrong: stop and report.

A decision that requires implementation names the issue that lands it: `Lands in:` followed by
the issue id or ids, ending the sentence with a period, for example `Lands in: `C7.12`.` A
decision that lands nothing writes `Lands in: none.` instead. The field is optional: not every
decision implies an issue, D21 is `site-owned` and lands nothing, so a blunt rule requiring the
field everywhere would be noise. `scripts/plan.py check` reads whichever decisions do carry the
field and verifies that every issue it names exists in the phase files, catching a decision that
names an issue that was renamed, split or never opened.

| # | Decision | Consequence for the plan |
|---|---|---|
| D1 | Separate repository and Python distribution named `community-base`, importable as `community_base`. Public, in the DataTalksClub organisation. No monorepo. | Sites pin a git tag in `uv.lock`; local development uses an editable path override. |
| D2 | The two sites never communicate with each other and never share a database, deployment, cache or credentials. Each installs the same package and runs it independently. | Nothing in the package may assume another site exists. No cross-site identifiers. |
| D3 | Background jobs move from django-q2 to Relay webhook tasks and Relay schedules. After migration a site runs no worker process. | `community_base.jobs` has two backends: `relay` (Relay client plus a signed ingress view) and `django_q` (thin wrapper, transitional). DTC uses `relay` from Phase 1; AISL uses `django_q` until D13 is satisfied. |
| D4 | Email: Relay owns templates, rendering, transport, contacts, subscriptions, campaigns and provider events. A site keeps a delivery projection, preference semantics, and Studio pages that proxy Relay. Historical send logs (AISL `EmailLog`, SES events, campaign deliveries) migrate into Relay; no site keeps a local email archive. | `community_base.mail` exposes one `send()` and two backends: `relay` (the target) and `ses_local` (AISL's markdown renderer and SES client, transitional under D13). AISL's templates move into Relay's catalog and its `EmailLog` history is imported into Relay in Phase 6, before the local table is dropped. |
| D5 | DataTalks.Club has no paid tiers. | Gating is a pluggable access policy. Shared models keep an integer `required_level`; DTC uses `0` (open) and `5` (registered) only. |
| D6 | Onboarding flows are configured per site and may differ per member group. | `community_base.onboarding` has a flow model with a selector by site configuration and group. |
| D7 | Events are authored in Studio and stored in the database on both sites, as AISL does today. DTC stops treating events as GitHub-owned content. | DTC imports its existing events once and removes events from its GitHub sync. |
| D8 | Courses support both cohort-based and self-paced modes on both sites. Homework, leaderboards and certificates (the coursework stack) are available to AISL as well. | One `curriculum` app with a course mode, one optional `coursework` app. |
| D9 | The package carries an admin API layer, starting with settings management, so both sites build on the same API. | `community_base.api` owns API keys, bearer auth, OpenAPI, and route registration. Shared apps ship their endpoints. |
| D10 | Apps lifted from AISL keep their Django app label where no collision exists (`events`, `notifications`, `comments`, `voting`, `questionnaires`, `community`). | AISL production tables and migration history are reused through a squashed initial migration with `replaces`. See `docs/03-playbooks.md`, playbook P4. |
| D11 | Every extraction that moves data models happens during a weekend freeze of the affected site. Preparation work (seam cutting) runs before the freeze with the site moving. | Each phase names its freeze. Freeze runbook is playbook P13. |
| D13 | AISL does not integrate with Relay until Relay has served DTC in production and is proven. Proven means: DTC production traffic on Relay for at least four consecutive weeks with no P1 incident attributed to Relay, and the Relay status contract green over that period. | AISL's Relay cutover for mail, jobs, campaigns and email-log history is Phase 6, the last phase. Until then AISL runs `community_base.mail` with the `ses_local` backend and `community_base.jobs` with the `django_q` backend. |
| D14 | Authentication and the user model are shared. The package ships a concrete `accounts.User` (label `accounts`, table `accounts_user`) plus login, registration, verification, password reset and social login. Site-specific member data lives in site extension models with a `OneToOneField` to the user. | AISL moves tier and Stripe fields from `User` to `payments.Membership` before adopting the shared model (Phase 3). DTC renames `CustomUser` to the shared `User` and moves course-platform fields to a `courses.LearnerProfile` extension. |
| D12 | Studio styling: the shared Studio shell ships AISL's Tailwind design (tokens, build, Lucide icons). DTC staff pages adopt that look. Public pages keep each site's own design system. | DTC's `templates/studio/base.html` is deleted in Phase 2. DTC's inline-stylesheet test applies to public pages only. |

D12 is the owner's default pending explicit confirmation. If the owner overrides it, Phase 2
issue C2.1 changes to a markup-only shell with a per-site stylesheet; everything else stands.

| # | Decision | Consequence for the plan |
|---|---|---|
| D15 | Amends D1, scoped to one CI job only. Owner-confirmed 2026-09-16 (relayed through the `DataTalksClub/website` orchestrator session), in response to the open question in issue C0.6. This package's own CI (`.github/workflows/cross-repo-check.yml`, playbook P16) may install this package's in-progress, unreleased state into a disposable checkout of `DataTalksClub/website` or `AI-Shipping-Labs/website` using that site's own P1 local-link tool, to run the site's real test suite against this package's current change on every push and pull request here. The site checkout is a sibling directory of the runner, resolved to that site's default branch at trigger time, discarded when the job ends; nothing is committed or pushed to the site repository. | D1's own text is unchanged: sites still pin a git tag in `uv.lock` for real installs, and local development still uses an editable path override. D0.2 is unchanged: each site's own CI (`scripts/check_community_base_source.py` in DataTalksClub/website, and its planned AI-Shipping-Labs counterpart) keeps failing closed against a local/path/branch package source, and a site pull request still never points at a branch or a local path (quality gate: "No local link committed"). Only this package's own CI, in this one job, is exempted. |

D15 does not make `cross-repo-check.yml`'s result a required GitHub branch-protection check on
this repository's pull requests; enabling that is a repository setting change, left to the owner
or orchestrator, not to this decision.

Taken by the owner on 2026-09-16, from the comparison recorded in
`docs/plan/evidence/site-convergence-analysis-2026-09-16.md`.

| # | Decision | Consequence for the plan |
|---|---|---|
| D16 | A wiki and a documentation section are shared capabilities, not DTC-only content types. AISL gains both so the two sites run the same implementation. | A package knowledge-base app owns the models, rendering and Studio screens; each site keeps its own parsers, routes and public templates. Phase 7 issues `C7.1`, `A7.1` and `D7.1`. |
| D17 | Neither site carries legacy URL or schema compatibility for its own history. Both products are new. AISL is in production, so its legacy constructs are removed through a staged rollout, not kept. | `events.EventAlias` and the alias route are removed from the package (`C4.1e`). DTC already dropped the model on 2026-09-11. No new alias, redirect-table or legacy-path model enters a shared app without a decision that names the history it preserves. |
| D18 | Confirms the public-pages clause of D12. Public templates and design systems stay site-owned and are never shared. | The package ships no public page markup beyond overridable defaults. The Studio clause of D12 is unchanged and still stands as written. |
| D19 | Optimistic concurrency and append-only model bases are a shared kernel concern. `RevisionedModel` and `AppendOnlyManager` move from DTC `core/models.py` into the package kernel. Owner-confirmed 2026-09-17, closing a `C7.1` undecided row. | About 100 lines, domain-agnostic, today at DTC `core/models.py:16-114` and used by eleven DTC models. The kernel has no model base classes yet, so this fills a real gap rather than duplicating one. The eleven consuming models stay DTC-owned. AISL keeps its pessimistic `select_for_update()` paradigm and is not required to adopt these. Phase 7 issue `C7.5`. |
| D20 | A queryable session record is a shared accounts concern. AISL's `AccountSession` moves into the package accounts app. Owner-confirmed 2026-09-17, closing a `C7.1` undecided row. | About 50 lines across AISL `accounts/models/session.py` and `session_backend.py`: a queryable view over Django's own session table, selected through `SESSION_ENGINE`, with two real call sites (GDPR erasure and expired-session cleanup). DTC has no equivalent and gains one. Phase 7 issue `C7.6`. |
| D21 | Article storage stays site-owned. The two sites keep their different article shapes and are not converged onto one model. Owner-confirmed 2026-09-17, closing a `C7.1` undecided row. | The genuinely shared part, the content sync engine, is already unified and both sites use it. AISL keeps its concrete `Article` model (about twenty fields, real `save()` logic, editable in Studio); DTC keeps `SyncedDocument`, generic across eleven content kinds. Forcing one shape would either lose AISL's editing workflow or force a restructure of DTC's other ten content kinds. No issue; the `C7.1` row reads `site-owned`. |
| D22 | Capability declaration for Studio and the admin API does not converge yet. Owner-confirmed 2026-09-17, deferring a `C7.1` undecided row rather than accepting or rejecting it. | DTC `core/capabilities.py` is 344 lines carrying forty-six concrete declarations plus an audit- and rate-limit-coupled dispatcher; AISL has no equivalent. The package already has two registries with different shapes, `api.registry.route` and `studio.registry.Destination`, so a third mechanism would have to reconcile them first. Only the generic mechanism is a candidate, and only after that reconciliation; DTC's forty-six declarations stay DTC-owned either way. The `C7.1` row reads `deferred`, which is a decided state and does not block the issue from closing. |
| D23 | One unified content format across both sites, specified in `docs/plan/evidence/unified-content-format-2026-09-17.md`. There is no backwards-compatibility requirement: every content file is rewritten. Owner-confirmed 2026-09-17. | A repository manifest `content.yaml` declares kinds; two file shapes only (markdown with required front matter, YAML manifests); nesting is directories and nothing else; ordering is an `NN-` name prefix; cross-references are markdown links with relative, `kind:slug` or external destinations. `[[wikilinks]]`, `prev_url`/`next_url`, Liquid and `{IMAGE:id}` tokens stop existing. Rendering is python-markdown at sync time through one nh3 sanitiser, with DTC's heading-id algorithm kept so pinned fragment contracts survive. Phase 7 issues `C7.7` to `C7.12`, then `A7.2`, `D7.2`, `D7.3`, `A7.3`, `D7.4`. Closes issue 253. |
| D24 | Amends the parser clause of D16. Parsers for the `wiki`, `docs` and `person` kinds move into the package, because the package owns their storage. Owner-confirmed 2026-09-17. | The article parser stays per site: article storage is site-owned under D21 and each site's parser is a thin adapter over the shared toolkit. The course parser already lives in the package. D16's wiki and docs capability clause is otherwise unchanged. |
| D25 | DTC's `_people` moves out of `datatalksclub.github.io` into `DataTalksClub/content` as a `people/` collection, and the Jekyll repository stops being a sync source. Owner-confirmed 2026-09-17. | Only one directory of that repository is synced today and the repository is being replaced anyway. |
| D26 | The FAQ repository is not converted beyond its `content.yaml` for now. Owner-confirmed 2026-09-17. | `faq` registers as a site kind with its current file shape. Adding `content_id` to about 1,400 questions and replacing `{IMAGE:id}` tokens waits until `faq_automation/`, which writes those files, is next touched. |
| D27 | The podwiki's `graph.json` and `search-corpus.json` stay opaque `data` files built by that repository's own scripts. Owner-confirmed 2026-09-17. | Rebuilding the graph from synced references is a DTC-owned follow-up after `D7.1` and `D7.4`, once every relation the graph needs is itself a synced reference. |
| D28 | The 25 DTC documentation pages under `courses/zoomcamp-logistics/` take the nested paths their section parents imply. Owner-confirmed 2026-09-17. | Their public paths change, which D17 permits: neither site carries legacy paths for its own history, and the section grouping is the authors' intent. |
| D29 | AISL's entitlement keys (`access_mode`, `enroll_url`, `program_label`, `maven_course_key`) stay AISL-only `extra` keys and are not a package concept. Owner-confirmed 2026-09-17. | They are promoted to first-class format keys when DTC or a second program needs them, not before. `COMMUNITY_BASE["COURSE_ACCESS_GRANTS"]` already gives a site the hook it needs. |
| D30 | AISL's `events/*.yaml` sync family is not retired by the content-format work. Owner-confirmed 2026-09-17. | D7 says events are Studio-authored on both sites, so the family does need to go, but as a separate AI-Shipping-Labs issue. The content-format issues do not carry an events decision. |
| D31 | The `person` kind is an optional instructor source, not a required one. Owner-confirmed 2026-09-17. | The format defines the shape; AISL decides in `A7.2` whether to replace its database-authored instructors with it. |
| D32 | Member API keys enforce their stored scopes. Owner-confirmed 2026-09-17, resolving the contradiction found in `A3.2c`. | This is a behaviour change on AI-Shipping-Labs, not a move: `member_api_key_required` currently ignores stored scopes and grants the full `SUPPORTED_SCOPES` set, and `member_api/tests/test_books_api.py::test_write_requires_write_progress_scope` pins that by asserting a `books:read` key succeeds on a `books:write_progress` route. That test inverts and its comment goes. Existing keys whose stored scopes are narrower than their use will start being rejected, so `A3.2c` inventories affected keys before the switch and the change ships on its own, not inside the credential move. |
| D33 | A release may be cut from `main` carrying the nine provisional kept-label migrations, under this scoped exception. Owner-confirmed 2026-09-17. | The append-only promise in `docs/PROCESS.md` attaches to `C5.3`, not to this tag, so `C3.7` and `C4.3` keep the right to rewrite those migrations; a site adopts the donor schema from `C5.3`, not from here. The exception is scoped to this one release train and does not repeal the rule. It is written down because the rule was already overridden silently at v0.4.0, v0.4.5, v0.4.6 and v0.4.7: recording the exception is stricter than the practice it replaces, not looser. The CHANGELOG entry carries the same warning so an adopting site reads it without finding this file. |
| D34 | A cohort manifest carries `title` explicitly. The unreachable default in the format's section 3.8 is removed rather than made reachable. Delegated to the orchestrator on 2026-09-17; the owner declined to rule and asked for a decision. | Section 3.3 makes `title` a required core key and `register_kind` refuses a part that overrides one, so the section 3.8 default could never apply: every real cohort manifest, which omits `title`, was rejected before a parser saw it. The alternative was an exemption letting a part default a core key. That was rejected because the core-key rule is absolute today, and an absolute rule is the thing that has caught seven specification errors this phase; a single exemption makes every future reader ask which keys are exempt. The cost falls on `C7.12`, which writes a `title` into each cohort manifest it converts, and the conversion is already rewriting every file. |
| D35 | A copied AI-Shipping-Labs operator token should carry a narrow scope naming the site's own operator API, not the wildcard `"*"`. This is a recommendation to that site, not a ruling binding it. Delegated to the orchestrator on 2026-09-17; the owner declined to rule and asked for a decision. | Issue 1656 prescribed `scopes=["*"]`. `community_base.api` is mounted on that site with twelve bearer routes covering settings, mail and content sync, and `APIKey.allows` short-circuits on the wildcard before comparing anything, so the copy as written would turn every existing operator token into a full settings, mail and content-sync credential. Verified twice, independently, in both repositories. The narrow scope is recommended because its failure direction is safe: if an operator token genuinely needs one of those package routes, it fails closed and is reported, where the wildcard fails open and is silent. Reversible by widening later with evidence of what actually broke. SCOPE OF THIS DECISION: this plan governs package behaviour. What scope AI-Shipping-Labs maps its own operator tokens to is a decision about credentials in that site's production database, and belongs to its owner on its own tracker (issue 1656), not to this file. Recorded here as the plan's recommendation and its reasoning so the site's ruling can be made on the merits; the credential move does not start until that ruling exists. AI-Shipping-Labs#1736 has since closed the account-merge half of this exposure, which concentrates rather than lightens it: the copy mapping is now the whole of it. |
| D36 | A copied operator token with no name takes the name `Operator token <short id>`. Delegated to the orchestrator on 2026-09-17. | `Token.name` is optional on the site and `APIKey.name` is required, so an unnamed token cannot be copied without one. Issue 1656 forbids Studio copy changes, and a fallback does change what the Studio list shows for an unnamed token: it reads `Operator token 3f2a1b` where it read a dash. That is accepted as the smaller harm, because the alternative is either refusing to migrate unnamed tokens or relaxing a package constraint that exists so a credential can be identified when it is revoked. Issue 1656 is amended rather than worked around. |
| D37 | `NullMediaStore` returns a site-absolute path. Delegated to the orchestrator on 2026-09-17. | It returns the repository path unchanged today, while the sanitiser admits an `img src` only when it is site-absolute or an absolute `http(s)` URL, so a site running the default backend stores synced images with no usable source and the failure is invisible until someone looks at a page. This is a behaviour change to a default, so it ships with its own test and a CHANGELOG line rather than inside another issue. |
| D38 | A cohort's `archive` key is a mapping carrying an optional notice path, not a boolean. Delegated to the orchestrator on 2026-09-17. | The format types it as a boolean; every real archived cohort carries `archive: {notice_path: ...}`, and two of the seventeen point at `cohorts/<year>/leaderboard.md` rather than `README.md`. Typing it as a boolean and only ever reading `README.md` would silently discard which file the notice is for those two. A mapping with an optional path costs one key and loses nothing. |
| D39 | The content format gains a fourth reference destination: a repository file that is not content. Delegated to the orchestrator on 2026-09-17. | Real lesson bodies link to `code/`, to `.py` and `.ipynb` files under `code/` and `embed/`, and to sibling cohort directories. None is a document or an asset, so under the default `strict_references` every one fails, which is why the course parser cannot yet resolve references at all. Such a destination resolves to the repository's hosting URL and is neither uploaded nor validated as content. The alternative, rewriting every such link during conversion, was rejected: it would bake one hosting provider's URL shape into thousands of content files. |
| D40 | An archived cohort is distinguished from a cohort with no placement by a field on `Cohort`, not by the emptiness of its placement. Delegated to the orchestrator on 2026-09-17. | An empty placement and an absent placement both leave zero `CohortModule` rows, so an archived cohort currently displays the course's whole tree. Fixing it needs a column, which touches the `C5.1e` placement contract. That contract is not reopened here: the decision is only that the distinction belongs on `Cohort`, and the schema change is its own issue, which must land before `D7.3` ships archived cohorts. |
